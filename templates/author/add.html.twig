<h1>Ajouter un auteur</h1>

<div style="max-width: 600px; margin: 40px auto; padding: 20px; border: 1px solid #ccc; border-radius: 8px; background-color: #fff;">

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <button type="submit" 
                style="margin-top: 10px; padding: 8px 16px; background-color: #007bff; color: white; border: none; border-radius: 4px;">
            Ajouter
        </button>
    {{ form_end(form) }}

</div>
