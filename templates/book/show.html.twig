<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>Book Details</title>
    <style>
        body { font-family: Arial, sans-serif; background-color: white; margin: 0; padding: 0; }
        h1 { text-align: center; margin-top: 30px; }
        .details { margin: 20px auto; width: 50%; font-size: 16px; }
        .details p { margin: 8px 0; }
        .btn {
            display: inline-block;
            padding: 8px 16px;
            background-color: #4CAF50; 
            color: white;
            text-decoration: none;
            border-radius: 4px;
            margin-top: 20px;
        }
        .btn:hover {
            background-color: #5a6268;
        }
        .center { text-align: center; }
    </style>
</head>
<body>

    <h1>Book</h1>

    <div class="details">
        <p><strong>Title of Book:</strong> {{ book.title }}</p>
        <p><strong>Publication date:</strong> {{ book.publicationDate|date('F j, Y H:i') }}</p>
        <p><strong>Author:</strong> {{ book.author ? book.author.username : 'No author' }}</p>
    </div>

    <div class="center">
        <a href="{{ path('book_list') }}" class="btn">Back to list</a>
    </div>

</body>
</html>
