<h1>Statistiques sur les livres</h1>

<p><strong>Nombre total de livres (DQL) :</strong> {{ nbrDQL }}</p>
<p><strong>Nombre total de livres (QueryBuilder) :</strong> {{ nbrQB }}</p>

<h2>Livres de l’auteur #1</h2>
<ul>
  {% for book in books %}
    <li>{{ book.title }} - {{ book.publicationDate|date('Y-m-d') }}</li>
  {% else %}
    <li>Aucun livre trouvé pour cet auteur</li>
  {% endfor %}
</ul>
